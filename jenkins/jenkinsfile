pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "Hello World!" 
                date
                netstat -ant | findstr 8080
            }
        }
    }
}
